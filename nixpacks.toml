[phases.setup]
nixPkgs = ["python312", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2"
